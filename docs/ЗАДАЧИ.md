# Задачи

## Текущий фокус

Собрать рабочий `V1`, который помогает получить первого платного клиента и не тонет в лишней архитектуре.

## Сделано

- создан репозиторий проекта
- проект опубликован в `GitHub`: `https://github.com/ViVaLaDaniel/gastroops-ai`
- подготовлен базовый каркас директорий и документации
- настроен домен `n8ndo.es`
- production перенесён на `Hetzner`
- подняты `caddy`, `n8n`, `scanner`, `redis`, `openclaw`
- выпущены `HTTPS`-сертификаты
- настроен доступ к `OpenClaw` через `token`
- подтверждён `device pairing` для рабочих браузеров
- создана инструкция по доступу
- создан `SOP` по работе с лидами
- создан `SOP` по delivery клиенту
- подготовлена схема `Google Sheets CRM` для листа `Leads`
- подготовлен blueprint workflow `form -> sheets -> telegram`
- лендинг переведён на русский и подготовлен под intake-форму
- `Obsidian`-система проекта и global hub синхронизированы в `GitHub`
- repaired global `MCP` path for hosted `Notion` and hosted `Linear`
- текущее состояние и blocker записаны в `Agent-Workspace-DB`

## Активные задачи

- привести repo-local инфраструктуру в соответствие с реальным production-стеком
- доработать лендинг под реальный оффер
- создать и подключить рабочую таблицу `Google Sheets CRM`
- собрать в `n8n` workflow `form -> sheets -> telegram` по blueprint
- привязать `Telegram bot` и `Google Sheets` credentials в `n8n`
- протестировать одну реальную заявку через форму
- подготовить шаблоны `mini audit`, `brief`, `proposal`
- создать внешний project mirror в `Notion`
- создать active execution item в `Linear`

## Следующие задачи

- настроить `Gmail -> draft reply -> approval`
- собрать `client folder` flow
- определить финальную структуру delivery для `Menu Pack`
- определить финальную структуру delivery для `Tech Cards + Food Cost Pack`
- подготовить 2–3 демо-примера для продаж

## Бэклог

- синхронизировать локальный `docker-compose` с production
- сделать единый deploy flow
- добавить мониторинг и backup-checklist в repo docs
- описать SOP по работе с delivery-артефактами глубже при росте проекта

## Что не делать сейчас

- не строить SaaS-кабинет
- не строить библиотеку-подписку раньше первых клиентов
- не автоматизировать `WhatsApp`
- не раздувать стек новыми сервисами без необходимости
